{
  "name": "custom-turf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify --bare index.js > dist.js && browserify --bare index.js | uglifyjs -c -m > dist.min.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-clipping-algorithm": "tchannel/greiner-hormann",
    "turf-difference": "tchannel/turf-difference#kill-jsts",
    "turf-intersect": "tchannel/turf-intersect#kill-jsts",
    "turf-union": "tchannel/turf-union#kill-jsts"
  },
  "devDependencies": {
    "browserify": "^10.2.4",
    "uglifyjs": "^2.4.10"
  }
}
